# Day - 09
- Written a script that creates an array of your favorite foods and then prints each item in the array on a new line.
- A function that takes an array as input and returns a new array with only the unique elements.
- A script that uses an associative array to store and retrieve key-value pairs (e.g., country names and their capitals).
