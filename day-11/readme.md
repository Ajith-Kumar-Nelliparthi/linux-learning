# Day - 11
sed (stream editor)
- script that uses sed to remove all HTML tags from a file.
- script that uses sed to comment out all lines in a configuration file that start with a specific word (e.g., "DEBUG")
- script that replaces color with colour in a given text file using sed
- script that extracts phone numbers from a file
- sed command to swap the first and second words of each line in a file.
