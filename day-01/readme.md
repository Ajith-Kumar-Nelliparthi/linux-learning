# Day - 01 Learnings
## Exercise - 1:
- **Navigation:**  `pwd`, `cd`, `ls -ltr`
- Created a dir called Day-01
- Moved to that dir using `cd`
- Created a empty file name 'python.py' using `touch python.py`
- Write hello world using `vim` and saved with `:x!`
- Executed with `chmod +x`

## Exercise - 2:
- Created a dir called "project"
- Inside project created 3 sub-dirs with `mkdir src docs tests`
- In each sub-dir, created an empty file called readme.md with `touch src/readme.md docs/readme.md tests/readme.md`

- **File Manipulation:**
- created a file called "todo.txt" in project dir
- Added a line "Bash Scripting" using vim
- Make a copy of todo.txt in "docs" dir with `cp todo.txt docs/`
- Renamed the copy using `mv todo.txt tasks.txt`

- **Search and Display:**
- Using `find readme.md` found all the readme files
- By using `grep -r "bash"` found the bash word in all existing files which are having them.
