# Day - 08
- Written a script that takes user input and appends it to a file. If the file doesn't exist, create it.

- Created a script that reads a list of numbers from a file, sorts them, and writes the sorted list to a new file.

- Written a script that executes a command, redirects its output to a file, and also displays the output on the screen.

- Created a script that uses a here document to generate a simple HTML file.

- Written a script that redirects errors to a log file, but still displays normal output on the screen.
