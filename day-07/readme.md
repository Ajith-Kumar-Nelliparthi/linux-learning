# Day-07
- Modified the script to accept a user's name as a command-line argument and included it in the greeting.

- Added error handling to the count_down function to ensure the input is a positive integer.

- Created a new function that generates a random number between 1 and 10, and used it in the main script logic.

- Modified the script to log its output to a file in addition to displaying it on the screen.

- Added a menu system to the script, allowing the user to choose between different functions to execute.
